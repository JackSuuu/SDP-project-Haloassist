# Core dependencies (Mac + Pi)
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0
torch>=2.0.0
torchvision>=0.15.0

# Hardware interface (for Raspberry Pi)
RPi.GPIO; platform_machine=="aarch64"
gpiozero; platform_machine=="aarch64"

# Speech-to-text
sounddevice>=0.4.6
vosk>=0.3.44

# Keyboard input fallback (when running without Raspberry Pi GPIO)
pynput>=1.7.0

# Development
pytest>=7.4.0
